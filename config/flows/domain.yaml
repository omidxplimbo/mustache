name: subdomain
description: Subdomain enumeration passive and active

steps:
  - scripts:
      - cat $projectName$-cero.txt | unfurl format %r.%t | sort -u > $projectName$-root-domain.txt
  - postRun:
      - $projectName$-root-domain.txt